language: java

sudo: false

git:
  submodules: false

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive
  - cd repseqio/milib && mvn clean install -DskipTests && cd ..
  - mvn clean install -DskipTests && cd ..

jdk:
  - openjdk7

cache:
  directories:
  - $HOME/.m2